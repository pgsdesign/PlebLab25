The git process is as follows<br>
Starting inside the correct folder that you set up for git. You'll see the hidden file in the folder if you need to confirm<br>
Use the commands 
git add .
git commit -m"commit message"
    you'll see stuff happen
git push
    this will update the GitHub files from the local files
git pull
    this will update the local files from GitHub
git clone 
    this will copy one down from GitHub


    Symantic HTML for DIVs
    <header>; <footer>, <article>, <section>, <nav>

    